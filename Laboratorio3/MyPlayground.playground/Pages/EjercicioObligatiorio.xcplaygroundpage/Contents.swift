/*func promedioVentas() {
    let ventasPorVendedor: [[Double]] = [
        [200.0, 150.0, 100.0],   // vendedor 1
        [300.0, 250.0],          // Vendedor 2
        [100.0, 200.0, 300.0, 400.0], // vendedorr3
        [500.0, 300.0, 300.0, 500.0] // vendedor4
    ]

    for (index, ventas) in ventasPorVendedor.enumerated() {
        let totalVentas = ventas.reduce(0, +)
        let promedio = totalVentas / Double(ventas.count)
        print("El promedio de ventas del vendedor \(index + 1) es: \(promedio)")
    }
}
*/
